export enum ShipmentStatus {
  SCHEDULED = 'Scheduled',
  IN_TRANSIT = 'In Transit',
  DELIVERED = 'Delivered',
  CANCELLED = 'Cancelled',
  DELETED = 'Deleted',
}

export enum DeliveryVehicleType {
  STANDARD = 'Standard Delivery Van',
  ECO_FRIENDLY = 'Eco-Friendly Vehicle',
}
